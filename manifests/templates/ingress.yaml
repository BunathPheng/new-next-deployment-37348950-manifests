---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: new-next-deployment-37348950                  # Ingress name
  labels:
    app: new-next-deployment-37348950
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "new-next-deployment-37348950.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: new-next-deployment-37348950               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
